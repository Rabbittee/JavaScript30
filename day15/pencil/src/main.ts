const addItems: HTMLElement | null = document.querySelector('.add-items');
const itemsList: HTMLElement | null = document.querySelector('.plates');
const items: Array<any> = [];

console.log('test ts file is here');